//const eventList = document.getElementById("event-list");

//// Creating the connection with the SignalR endpoint defined on the class Startup.cs
//const connection = new signalR.HubConnectionBuilder().withUrl("/DockerHost").build();

//connection.on("NewStat", containerStat => {
//    console.log(containerStat);
//});
